# tourista-turbo

Minimal Turborepo scaffold for `tourista` and `touring-demo`.

## Workspace layout

- `apps/touring-demo`: demo application
- `packages/tourista`: library package

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm lint
pnpm type:check
```
